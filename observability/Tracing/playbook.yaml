- name: Deploy Jaeger tracing to Kubernetes
  hosts: all
  gather_facts: false
  tasks:
    - name: Apply Jaeger manifests
      kubernetes.core.k8s:
        state: present
        src: "{{ item }}"
      loop:
        - jaeger/namespace.yaml
        - jaeger/jaeger.yaml
